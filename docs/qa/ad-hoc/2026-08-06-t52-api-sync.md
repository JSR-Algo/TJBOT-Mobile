# adhoc-2026-08-06-t52-api-sync — Verification Matrix

**Repo:** tbot-mobile · **Date:** 2026-08-06 · **Task:** T5.2 (backend ↔ mobile API contract sync)
**Branch:** `lesson-prod/t52-api-sync` · **Gate:** see [`t52` in GATE_LOG.md](../../../../lesson-prod/GATE_LOG.md)
**Status:** all five deep-dive boxes walked — 10 unserved routes resolved, 2 payload
defects fixed, the verify command replaced with a real gate; 6 out-of-scope findings routed

## Scope

Backend `openapi.json` (read-only — see "The backend contract is current" below);
mobile `src/services/api/*` and `undocumented-api-routes.ts`.

## Headline: the verify command was a no-op

`npm run api:contract-sync:check` ran `scripts/check-api-contract-sync.mjs`, whose
entire body was:

```js
// CI gate placeholder — passes when no drift detected.
process.exit(0);
```

It read no file and reported "no drift detected" unconditionally. T5.2's Done
criterion ("contract check passes clean") was therefore already satisfied by a
script that could not fail, while mobile shipped **ten** call sites addressing
routes the backend has never served. Doing the task at all required implementing
the gate first; the drift below is what it found on the first honest run.

## The backend contract is current

`npm run openapi:generate` in `tbot-backend` (which runs `nest build` first)
reproduced `openapi.json` **byte-identically** — 130 paths, 146 operations, plus
75 modular routes under `x-tbot-modular-route-contract`. The committed artifact
is not stale, so no backend change was needed. The one diff the regeneration
produced is a docs artifact and is routed as F-T52-07 below.

### Reading the contract correctly

The contract has two artifacts and neither alone answers "is this route served?":

| Artifact | Contains | Caveat |
|---|---|---|
| `openapi.json` → `paths` | Nest routes under the `/v1` global prefix | `generate-openapi.mjs` filters to `IN_SCOPE_TAGS` = auth, households, devices, notifications, admin, internal, lessons. A **served** route outside those tags is absent here. Absence means *undocumented*, never *unserved*. |
| `openapi.json` → `x-tbot-modular-route-contract.routes` | the modular Express runtime | Only the subset matched by `isBridgeRoute()` (`src/modules/app/modular-route-bridge.ts`) is actually mounted. The rest are **declared but unrouted**, so they 404 exactly like an undeclared route. |

Both traps produce false results if ignored: a first pass using `paths` alone
flagged 50 of 102 mobile calls as broken (wrong — most are served but untagged),
and a second pass that trusted the modular contract's declarations missed that
`POST /v1/children/{id}/archive` is declared yet never bridged. The final gate
scans the Nest `@Controller` decorators directly for "served" and mirrors
`isBridgeRoute()` for the modular half.

## Defects found and fixed

### 1. Ten mobile call sites addressed routes that can only 404 (HIGH)

Five had an exact contract equivalent under a different name — the mobile
behaviour was already written, only the URL was wrong:

| # | Mobile called | Backend serves | Reachable from |
|---|---|---|---|
| 1 | `GET /v1/orders/{id}` | `GET /v1/billing/orders/{id}` | `OrderConfirmScreen:31` (production) |
| 2 | `POST /v1/orders/{id}/cancel` | `POST /v1/billing/orders/{id}/cancel` | `SubscriptionsScreen:131` (production) |
| 3 | `POST /v1/orders/{id}/return` | `POST /v1/billing/orders/{id}/return-request` | exported, no caller |
| 4 | `POST /v1/billing/subscription/reactivate` | `POST /v1/billing/reactivate` | `SubscriptionsScreen:219` (production) |
| 5 | `POST /v1/children/{id}/archive` | `PATCH /v1/identity/children/{id}` `{status:'archived'}` | exported, no caller |

Row 5 is the subtle one: `POST /v1/children/:childProfileId/archive` **is** in the
modular route contract, so a declaration-only check calls it documented. But
`isBridgeRoute()` forwards only `/v1/billing/*`, the entitlements internal, the
Stripe webhook, and two notification routes — `/v1/children/*` is never mounted.
Archiving is a status transition on the ADR-0011 D1 route that `deleteChild`
already uses; any household parent may drive it and it is idempotent.

Five had no backend contract at all and now fail closed on the
`BACKEND_CONTRACT_UNAVAILABLE` sentinel instead of issuing a request that cannot
succeed: `getShippingStatus`, `activateRobot`, `refreshEntitlementsAfterPurchase`,
`getParentToday`, `getParentHistory`.

`POST /v1/device/activate` deserves a note — it exists, so a path-similarity check
would "fix" `activateRobot` onto it. It is the OTA device alias: it authenticates
by `device-id`/`client-id` headers and accepts no activation code, so it cannot
serve a consumer activation flow. Left uncontracted deliberately.

### 2. `mapOrder` read fields the orders module does not emit (HIGH)

`OrdersService.orderResponse` answers `{ data: { orderId, state, stateVersion } }`.
The mapper read `raw.id` and `raw.status`, so `Order.id` and `Order.status` were
`undefined` behind a `string`/union type — and `OrderConfirmScreen`'s
`next.status === 'paid'` gate could never fire, so the post-purchase entitlement
refresh never ran. This is a second, independent defect: fixing only the URL
(defect 1) would have left the screen silently wrong rather than obviously broken.

The two order vocabularies also differ. Backend `OrderState` is
`created | paid | fulfilling | shipped | arrived | activated | cancel_pending |
cancel_failed | cancelled | refunded`; mobile `Order['status']` is
`pending | confirmed | shipped | delivered | paid | pending_payment | cancelled |
refunded`. They overlap on four names. `mapOrderStatus` now translates explicitly
rather than casting, so an unmapped backend state cannot masquerade as a mobile
one; states mobile has no name for collapse to `pending`, which preserves the only
live consumer exactly. Widening the mobile union is purchase-feature work (F-T52-05).

### 3. Three copies of one error class (MED — deep-dive box 4)

`BackendContractUnavailableError` was declared independently in
`undocumented-api-routes.ts`, `parent.api.ts`, and `purchase.api.ts`. All three
carried `code: 'BACKEND_CONTRACT_UNAVAILABLE'`, so `code`-based handling worked,
but `instanceof` was **false** across module boundaries — three nominal types
behind one code. `parent.api.ts` and `purchase.api.ts` now re-export the canonical
class.

Separately, `parent.api.ts`'s `getSafetyConfig`, `updateSafetyConfig`,
`getSettings` and `updateSettings` rejected with a bare
`new Error('not implemented')`. That carries no `code`, so `normalizeError`
mapped it to `UNKNOWN_ERROR` — indistinguishable from a genuine server fault. All
four now use the typed sentinel.

### 4. `undocumented-api-routes.ts` justified nothing (the Done criterion)

The file held only the error class and helper. The 28 operations that threw
through it carried no record of *why*, so "documented in the backend contract or
removed/justified" could not be evaluated. It is now a typed registry —
**38 rows**, one per operation, each with `module`, `attemptedRoute`, `status`,
`reason`, and (where applicable) `owner`.

`status` is what stops the file rotting, and the gate enforces it in both
directions:

- `no-backend-contract` (29 rows) — the gate asserts the route really is absent.
  The day the backend ships one, the gate goes **red** and forces a revisit.
- `backend-route-exists` (9 rows) — the backend serves it, but wiring mobile is
  feature work; `owner` is mandatory and names the §5 row tracking it. The gate
  asserts the route is still present.

The nine `backend-route-exists` rows are honest debt, not silence: `createOrder`
(`POST /v1/billing/orders`), `controlsApi.get/updateControls` (`/v1/controls/{id}`),
the four `dashboard.ts` summaries (`/v1/summaries/*`), and `getFirmwareVersion` /
`runFirmwareUpdate` (`/v1/robots/{id}/ota*`). Each is routed below.

## What the new gate checks

`scripts/check-api-contract-sync.mjs` (replacing the placeholder) fails on:

1. a `client.<verb>(...)` route in `src/services/api/**` that nothing serves;
2. a `backendContractUnavailable(op)` with no registry row;
3. a registry row whose `status` no longer matches backend reality (both directions);
4. a `backend-route-exists` row with no `owner`;
5. an orphaned registry row (declared but never thrown).

It also self-checks: it aborts rather than passing vacuously if it scans zero
backend routes or zero mobile calls, and it fails if its mirror of
`isBridgeRoute()` drifts from the backend source. Served-but-undocumented routes
are reported as informational notes (24 today — see F-T52-08), not failures,
because `paths` is tag-filtered by design.

## Deep-dive checklist

| # | Box | Result |
|---|---|---|
| 1 | `api:contract-sync:check` green against CURRENT `openapi.json` | **PASS** — after implementing the gate; `openapi.json` verified current by byte-identical regeneration |
| 2 | Every route in `undocumented-api-routes.ts` documented or removed | **PASS** — 38 rows, each justified; 5 call sites repointed onto the contract, 5 converted to justified stubs |
| 3 | Response nullability: fields mobile treats as required are non-nullable in contract (sample-decode fixtures) | **FAIL → fixed** — `mapOrder` (defect 2). Fixtures in `tests/api/contract-sync.test.ts` decode the real `orderResponse`/refund bodies and assert no required field is undefined |
| 4 | Error envelope consistent across lesson endpoints (mobile parses one shape) | **FAIL → fixed** — three duplicate error classes collapsed to one; four bare `Error` throws typed (defect 3). `normalizeError` already handled both backend envelope shapes and needed no change |
| 5 | Removed/renamed fields since last app release flagged → feeds T7.4 | **PASS** — `orderId`/`state` renames fixed; `productId`/`totalCents`/`receiptEmail`/`cancelledAt` have no contract field and the order-state enums diverge → F-T52-05 |

## Verification

### RED → GREEN

Repro: [`lesson-prod/repros/t52.sh`](../../../../lesson-prod/repros/t52.sh), two phases —
(A) the contract gate, (B) the jest regressions. Both artifacts are materialized
from `main` first, falling back to immutable snapshots in `lesson-prod/repros/t52/`;
the repro never references `lesson-prod/t52-api-sync`, so Ship-checklist branch
deletion cannot break it (plan §5 repro-vs-branch-deletion rule).

The materializer requires a **content marker**, not just a non-empty file. Before
this merge, `main:scripts/check-api-contract-sync.mjs` is the `process.exit(0)`
placeholder — non-empty, so an emptiness check accepted it and phase A passed
vacuously on *both* sides of the gate. Caught while proving RED; worth repeating
for any repro that materializes a file it is also fixing.

**RED @ base `f33a0dec`** — phase A alone reports 40 findings: the 10 unserved
routes above, plus 30 unjustified stubs (registry rows: 0). Phase B fails on the
route and payload assertions.

**GREEN @ tip** — phase A passes; phase B 26/26.

### Suites (branch tip)

| Suite | Result |
|---|---|
| `npm run typecheck` | clean |
| `npm run lint` | clean (`--max-warnings=0`) |
| `npm test` | 226 passed / 1 failed / 1 skipped suites · 2595 passed, 1 failed, 19 skipped — the single failure is **pre-existing on main** (F-T52-09) |
| `npm run test:state-machines` | 10 suites / 196 tests passed |
| `npm run test:screens` | 71 suites / 941 tests passed |
| `npm run api:contract-sync:check` | PASS — 283 served ops, 201 documented, 102 mobile calls, 38 registry rows |

The `npm test` failure is `tests/features/progress/t33-dashboard-parity-and-freshness.test.tsx`.
It reproduces on `main` at `f33a0dec` with the branch not applied, so it is not this task's.
A rebased re-run of the same suite at the same tip then passed **2596/2596** — the case is
order-dependent, which is the substance of F-T52-09. Both runs are recorded rather than the
convenient one.

### Ship checklist (step 4 — re-test on the main checkout)

Merged as `625c2cf5` (merge #21 — `21 % 5 ≠ 0`, so the every-5-merges integration re-gate did
not run; that re-gate is separately known-broken, F-T41E-01). Mobile-only change, no deployable
code touched, so **no deploy** (step 3): backend is byte-identical, and the mobile release
decision belongs to T7.4.

| Suite (on `main` `625c2cf5`) | Result |
|---|---|
| `npm run typecheck` | clean |
| `npm run lint` | clean (`--max-warnings=0`) |
| `npm test` | **227 suites passed / 1 skipped · 2596 passed, 0 failed**, 19 skipped |
| `npm run test:state-machines` | 10 suites / 196 tests passed |
| `npm run test:screens` | 71 suites / 941 tests passed |
| `npm run api:contract-sync:check` | PASS — 283 served ops, 201 documented, 97 client calls, 38 registry rows |
| `lesson-prod/repros/t52.sh` on `main` | exit 0 — phase A PASS, phase B 26/26; checkout left clean |

The client-call count drops 102 → 97 because five call sites became justified stubs; no call
site was deleted.

**Re-verified after T5.1 merged** (backend `7845f4e`): T5.1 touched neither `openapi.json`, nor
`modular-route-bridge.ts`, nor `backend-routes.ts`, and the gate still reports 283 served ops
and PASS. This matters because the gate reads the backend checkout live rather than a snapshot,
so a backend merge can change its verdict without any mobile change.

Worktree removed and `lesson-prod/t52-api-sync` deleted after confirming the tree was clean and
`git merge-base --is-ancestor` held. No remote branch existed. `tbot-mobile` `main` is 41 commits
ahead of `jsr/main` — a campaign-wide backlog, not this task; `merge-task.sh` reserves pushing as
a deliberate human step.

---

# Second pass — findings §5 had already routed TO T5.2

The first pass closed the task's own checklist but never looked at the eleven §5
rows whose **owning task is T5.2**. Working them found a hole in the first pass's
own gate, and two places where the first pass documented the backend wrongly.

## 6. A decorator is not a contract — the gate's own blind spot (HIGH)

`CourseLibraryController` keeps handlers whose entire body is
`throw new HttpException(retiredBody(), 410)`:

```ts
@Post(':courseId/unlock')
unlockCourse(): never { throw new HttpException(retiredBody(), 410); }
```

The first-pass gate scanned `@Get`/`@Post` decorators to answer "is this
served?", so it counted `unlock`, `send-to-robot` and `sync-status` as served
and passed. **A route that always returns 410 is exactly as unusable as a 404** —
the gate was green over three mobile calls that could only fail.

Fixed both sides. `isRetiredHandler()` classifies a handler whose first
statement is an unconditional Gone throw into a separate `retired` map, and a
mobile call landing there fails as `[retired-route]` with the replacement the
backend names. Served-op count drops 283 → 280, which is the measure of the
blind spot. The three mobile shims are deleted: they had no caller in `src/`
and were `@deprecated` no-ops kept "so older callers fall through gracefully",
which a 410 does not do. Live replacements are `enrollCourse` and
`createAssignment`.

This also falsified two of the first pass's own registry justifications, which
asserted those retired routes were *live* (`purchaseCourse` and
`pushCourseToDevice`). Both corrected. A justification is only as good as the
backend fact it cites, and the first pass never checked whether a "served"
handler actually does anything.

## 7. Fifteen more bare `Error('not implemented')` throws (MED)

The first pass's box-4 claim ("four bare `Error` throws typed") was true but
incomplete: it audited functions that already used the sentinel plus `client.*`
call sites, and never grepped for bare throws. Fifteen remained across
`support.api.ts` (2), `home.api.ts` (2), `robot-mgmt.api.ts` (6) and
`progress.api.ts` (4) — each carrying no `code`, so each normalized to
`UNKNOWN_ERROR`, indistinguishable from a real server fault. All are now on the
sentinel with a verified registry row; the registry grows 38 → 53.

`getProgressSummary` was worse than a bare throw: it **resolved** a frozen
all-zero `ProgressSummary`, so a caller could not tell "this child has no data
yet" from "this surface has no backend contract" while every sibling in the file
threw. It now fails closed.

Checking each against the backend moved eight of the fifteen to
`backend-route-exists` (the `/v1/summaries/*` family, `/v1/robots/{id}/status`,
`/v1/devices/{id}/factory-reset`, `/v1/learning/children/{id}/vocab{,/due}`) —
all device- or child-scoped routes that these zero-argument functions cannot
call without a parameter and a mapper.

## 8. One operation name, two backend surfaces (MED)

`getReviewQueue` is exported by both `course.api.ts` (user-scoped, no route
exists) and `progress.api.ts` (child-scoped, `/v1/learning/children/{id}/vocab/due`
exists). The registry is keyed by operation name, so one row would have
justified both — and whichever row was written would have been wrong about one
of them. Names are now module-qualified (`course.getReviewQueue`,
`progress.getReviewQueue`) and the gate fails on any operation thrown from more
than one module.

## 9. The error envelope had a third shape (MED — closes §5's T1.6 re-route)

`normalizeError` handled `{error:{code,message}}` and `{code,message}`. The
backend also emits **`{error:'CODE_STRING'}`** — from the retired 410 body
(`{error:'ENDPOINT_RETIRED', useInstead}`) and from
`device-assignment.controller.ts:321` (`{error:'PARENT_TOKEN_REQUIRED'}`), which
sits ten lines from a sibling using `{code:'DEVICE_FORBIDDEN'}`. A string
`error` failed Shape 1's `typeof === 'object'` test, failed Shape 2's `data.code`
test, failed Shape 3's `data.message` test, and landed on the bare-status
handler — so a 403 read as a generic `FORBIDDEN` and the actual code was lost.

Shape 1b now reads it. This is a mobile-side tolerance fix; normalizing the
backend to one envelope is routed as **F-T52-10**.

## Second-pass verification

**RED @ base `f4435e44`** (main after the first pass) — phase A fails with the
3 `[retired-route]` findings and, under `set -e`, aborts there. So phase B was
run separately against the same base to prove it is not carried by phase A:
**20 of 48 fail** — the deleted-export check, all 15 sentinel conversions plus
`getProgressSummary`, and 3 of the 5 envelope cases (the two already-supported
shapes pass, which is the control).

The count is worth stating precisely because "phase A red" alone would not show
that the jest half reproduces anything.

**GREEN @ tip** — phase A PASS (280 served ops, 53 registry rows, 16 awaiting
mobile wiring); phase B 48/48.

| Suite (branch tip) | Result |
|---|---|
| `npm run typecheck` | clean |
| `npm run lint` | clean (`--max-warnings=0`) |
| `npm test` | 227 suites / 2618 passed, 19 skipped |
| `npm run api:contract-sync:check` | PASS |

`npm test` was run **3×** on the branch and **3×** on unmodified `main`: both
show the identical 2-of-3 failure of `t33-dashboard-parity-and-freshness`
(`1 failed` on runs 1 and 3, clean on run 2). Same rate, same test, with and
without this branch — F-T52-09 is pre-existing and is now characterised rather
than just asserted.

### Second-pass ship checklist (step 4 — re-test on the main checkout)

Merged as `7d53a52c` (merge #24). Mobile-only again, backend untouched, so no
deploy. Merge **#25 will trigger the every-5-merges integration re-gate**, which
re-runs every accumulated repro on main — both T5.2 repros were therefore run
there explicitly rather than assumed:

| Suite (on `main` `7d53a52c`) | Result |
|---|---|
| `npm run typecheck` | clean |
| `npm run lint` | clean (`--max-warnings=0`) |
| `npm test` | **227 suites passed / 1 skipped · 2618 passed, 0 failed**, 19 skipped |
| `npm run test:state-machines` | 10 suites / 196 tests passed |
| `npm run test:screens` | 71 suites / 941 tests passed |
| `npm run api:contract-sync:check` | PASS — 280 served ops, 53 registry rows (16 awaiting mobile wiring) |
| `repros/t52.sh` on `main` | exit 0 |
| `repros/t52b.sh` on `main` | exit 0 |

Both repros leave the checkout clean, and both declare an **absolute** `# repo:`
header, so the re-gate's `cd` resolves — the failure mode behind the false
REGRESSION storms recorded in F-T41E-01 and merge #15.

The `repros/t52/` snapshots were refreshed to match `main` after this pass. They
had held first-pass artifacts, and that particular staleness was dangerous: the
first-pass gate did not know a handler unconditionally throwing 410 is unusable,
so if the `main` lookup ever failed, `t52.sh` would have fallen back to a gate
that passes where it should fail. A fallback that silently passes is the same
class of defect this task exists to remove.

Worktree removed and `lesson-prod/t52-retired-routes` deleted after confirming
the tree was clean and `git merge-base --is-ancestor` held. No remote branch
existed.

## Disposition of the eleven §5 rows owned by T5.2

| §5 row | Disposition |
|---|---|
| `check-api-contract-sync.mjs` is a vacuous stub (HIGH) | **RESOLVED** — first pass |
| retired `unlockCourse`/`sendCourseToRobot` shims (LOW) | **RESOLVED** — deleted; gate now catches the class |
| `getProgressSummary` resolves fake zeros (LOW) | **RESOLVED** — fails closed |
| `DEVICE_FORBIDDEN`/`PARENT_TOKEN_REQUIRED` envelope (MED) | **PARTIALLY RESOLVED** — mobile parses both shapes; backend normalization → F-T52-10 |
| `lesson-session.api.ts` has no session-status query (HIGH) | **DOCUMENTED, re-routed** — all five stubs justified; the ask is a *new backend endpoint*, not contract sync |
| modular route contract excludes the lesson stack (LOW) | **merged into F-T52-08** — same root cause |
| `course` browse stack is a static mockup (HIGH) | **re-routed** — screens, not API clients; the six catalog stubs are now registered, which is the API-side half |
| `CourseLockedScreen` hardcoded fallback (MED) | **re-routed** — screen logic |
| `LessonResumeScreen` cold-start recovery (HIGH) | **re-routed** — bootstrap/navigation integration |
| reconnect restarts from `lesson_prepare` (MED) | **re-routed** — ESP/product decision |
| folder entity absent (MED) | **re-routed** — backend schema decision |

## Findings routed out of scope (LESSON_PRODUCTION_PLAN.md §5)

| ID | Severity | Summary |
|---|---|---|
| F-T52-01 | MED | `createOrder` stubbed while `POST /v1/billing/orders` is live and bridged |
| F-T52-02 | MED | `controlsApi.get/updateControls` stubbed while `/v1/controls/{deviceId}` is live |
| F-T52-03 | MED | four `dashboard.ts` reads stubbed while `/v1/summaries/*` is live |
| F-T52-04 | LOW | `getFirmwareVersion`/`runFirmwareUpdate` stubbed while `/v1/robots/{id}/ota*` is live |
| F-T52-05 | MED | order payload: `productId`/`totalCents`/`receiptEmail`/`cancelledAt` have no contract field; the two order-state enums diverge |
| F-T52-06 | MED | `OrderConfirmScreen` turns an entitlement-refresh failure into a whole-screen error |
| F-T52-07 | LOW | `openapi:generate` deletes a hand-added `scopeNote` from a generated docs artifact, so `ci:required` dirties the tree |
| F-T52-08 | HIGH | 24 mobile-facing routes are served but absent from `openapi.json` — the lesson stack's whole mobile surface is undocumented |
| F-T52-09 | MED | `t33-dashboard-parity-and-freshness` pagination test fails on `main`, order-dependently (2 of 3 runs, identical with and without this branch) |
| F-T52-10 | MED | backend emits three error-envelope shapes; `{error:'CODE'}` sits beside `{code:'CODE'}` in one controller |

**Not fixed here, deliberately.** F-T52-08 is the largest: `course-library`,
`learning/children/*`, `mobile/children/*/learning-status`, `children/*/lesson-progress`,
`devices/*/preload-status` and `courses/*/enroll` — precisely the lesson stack's
mobile surface — are all served but undocumented. Documenting them means widening
`IN_SCOPE_TAGS` in `generate-openapi.mjs`, which the generator's own comment says
would "force the strict TBot Spectral ruleset ... against operations that were
never in this task's scope" and break `npm run openapi:lint` (part of
`ci:required`) across ~7 controllers. That is its own task, not a T5.2 side
effect. **T5.2 makes the mobile client honest about what the backend serves; it
does not make the backend document what it serves.**

## Files changed

| File | Change |
|---|---|
| `scripts/check-api-contract-sync.mjs` | placeholder → real two-artifact gate |
| `src/services/api/undocumented-api-routes.ts` | typed 38-row justification registry |
| `src/services/api/purchase.api.ts` | 4 routes repointed; `mapOrder`/`mapOrderStatus` fixed; 2 stubs; error class re-exported; dead shipping mapper removed |
| `src/services/api/parent.api.ts` | 2 stubs; 4 bare `Error` throws typed; error class re-exported; dead mappers removed |
| `src/services/api/account.ts` | `refreshEntitlementsAfterPurchase` fails closed |
| `src/services/api/households.ts` | `archiveChild` repointed to the ADR-0011 status route |
| `tests/api/contract-sync.test.ts` | **new** — 26 regressions incl. sample-decode fixtures |
| `tests/api/account-entitlements.test.ts` | rewritten to the fail-closed contract |
| `tests/api/households.test.ts` | archive expectation updated to the real route |

Backend: **no change**. `openapi.json` verified current, not edited.
