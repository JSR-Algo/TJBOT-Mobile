# T3.2 Architecture-Aligned Closeout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correct T3.2's evidence and campaign tracking so its DONE status matches the accepted robot-owned lesson architecture without adding T5.2 or changing production behavior.

**Architecture:** Preserve the shipped lesson-session model and keep all 24 phone-runtime routes production-hidden. Reclassify no-runtime observations as intentional ADR 0006 constraints, while leaving authoritative production recovery and observer re-attachment explicitly outside T3.2.

**Tech Stack:** Markdown evidence and campaign records, Git worktrees, existing Jest/TypeScript/ESLint verification and T0.4 gate tooling.

---

## File Map

- Modify `docs/qa/ad-hoc/2026-08-06-t32-mobile-session-machine.md`: authoritative T3.2 evidence correction and fresh closeout verification.
- Modify `/Users/manhhodinh/Documents/TBOT/lesson-prod/t32-mobile-session-machine.md`: task status narrative and deep-dive classification.
- Modify `/Users/manhhodinh/Documents/TBOT/LESSON_PRODUCTION_PLAN.md`: T3.2 board evidence text and findings ownership/status corrections.
- No changes under `src/` or `tests/`.

### Task 1: Correct T3.2 Evidence

**Files:**
- Modify: `docs/qa/ad-hoc/2026-08-06-t32-mobile-session-machine.md`

- [ ] **Step 1: Correct the evidence status and architectural premise**

Change the opening status from `PARTIAL` to an architecture-aligned `DONE` statement. Replace the claim that the missing runtime consumer is an implementation gap with a citation to ADR 0006: the robot owns the production lesson runtime, the phone-runtime screens remain production-hidden, and the production phone surface is an observer.

- [ ] **Step 2: Reclassify runtime-only deep-dive rows**

For renderer debounce, foreground re-sync, active phone audio interruption, and listener duplication, retain the model-side evidence and classify the renderer-side requirement as `NOT APPLICABLE TO T3.2 PRODUCTION SCOPE`. State the appropriate production owner without claiming it is implemented.

- [ ] **Step 3: Replace the honest-gaps section**

Rename it to `Architecture constraints and routed production work`. Preserve the historical facts, cite ADR 0006, and explicitly state that T3.4 remains blocked without authoritative observer/session integration.

- [ ] **Step 4: Add closeout correction evidence**

Append the approved design link, branch/base identifiers, baseline verification results, and a statement that `git diff` contains no production or test changes.

- [ ] **Step 5: Validate and commit**

Run:

```bash
git diff --check
rg -n "PARTIAL|UNVERIFIABLE \(no runtime\)|runtime consumer" docs/qa/ad-hoc/2026-08-06-t32-mobile-session-machine.md
git diff -- src tests
```

Expected: diff check passes; remaining historical terms are either removed or explicitly contextualized; the `src`/`tests` diff is empty.

Commit:

```bash
git add docs/qa/ad-hoc/2026-08-06-t32-mobile-session-machine.md
git commit -m "docs(lesson-session): align T3.2 evidence with runtime ownership" -m "Refs: T3.2"
```

### Task 2: Correct Campaign Tracking

**Files:**
- Modify: `/Users/manhhodinh/Documents/TBOT/lesson-prod/t32-mobile-session-machine.md`
- Modify: `/Users/manhhodinh/Documents/TBOT/LESSON_PRODUCTION_PLAN.md`

- [ ] **Step 1: Correct the task status narrative**

Keep T3.2 `DONE`, but replace the statement that the feature lacks a runtime consumer as an incomplete condition. State that the 24 phone-runtime screens are intentionally production-hidden by ADR 0006 and that the verified model remains a demo/spec surface.

- [ ] **Step 2: Correct deep-dive classifications**

Mark renderer debounce, background/foreground re-sync, phone-call audio interruption, and listener duplication as architecture-scoped rather than incomplete. Keep all model evidence and do not claim observer behavior is implemented.

- [ ] **Step 3: Correct findings ownership without deleting history**

Update the T3.2 no-runtime finding to `CLOSED — reclassified by ADR 0006`. For T3.4 blocker rows that name T3.2, remove T3.2 as owner where the accepted architecture assigns the work to the production observer/course-library, fallback, auth, or T5.2 integration. Keep unresolved production blockers `OPEN`.

- [ ] **Step 4: Validate root records**

Run:

```bash
rg -n "\| T3\.2 \||T3\.2 deep-dive|T3\.4 final review" /Users/manhhodinh/Documents/TBOT/LESSON_PRODUCTION_PLAN.md
sed -n '1,130p' /Users/manhhodinh/Documents/TBOT/lesson-prod/t32-mobile-session-machine.md
```

Expected: board and task both say DONE; no record claims T5.2 or T3.4 is complete.

### Task 3: Review and Verify the Closeout

**Files:**
- Review all changed files from Tasks 1-2.

- [ ] **Step 1: Run focused verification at branch tip**

Run:

```bash
npm run test:state-machines
npm run test:navigation
npm run typecheck
npm run lint
```

Expected: all commands exit 0 with non-zero test counts.

- [ ] **Step 2: Run the repository standard suite**

Run:

```bash
npm test
```

Expected: all runnable unit suites pass; any known load flake must be reproduced on unchanged main before it can be treated as unrelated.

- [ ] **Step 3: Perform spec-compliance review**

Confirm:

```text
- No source or test files changed.
- T3.2 remains DONE.
- ADR 0006 is cited accurately.
- No T5.2 implementation or completion claim exists.
- T3.4 remains blocked where authoritative production recovery is missing.
```

- [ ] **Step 4: Perform code-quality/document-quality review**

Check for contradictory statuses, duplicated findings, stale links, unsupported claims, and Markdown formatting errors. Resolve every finding before shipping.

### Task 4: Run the T3.2 Ship Checklist

**Files:**
- Modify: `docs/qa/ad-hoc/2026-08-06-t32-mobile-session-machine.md` with final Ship evidence.

- [ ] **Step 1: Rebase and re-verify**

Rebase the branch on latest local `main`, rerun Task 3 verification, and confirm the worktree is clean after committing final evidence.

- [ ] **Step 2: Gate and merge**

Use the T0.4 T3.2 gate/repro if it can distinguish this documentation-only correction from base. If the historical behavioral repro is green on both sides by design, record that the behavioral gate was already VERIFIED for the unchanged implementation and use the repository's merge protocol without fabricating a RED phase.

- [ ] **Step 3: Deploy decision**

Record `none`: this is mobile documentation-only and changes no application bundle.

- [ ] **Step 4: Re-test on main**

After merge, run:

```bash
npm run test:state-machines
npm run test:navigation
```

Expected: 10 state-machine suites and 26 navigation suites pass with the current test counts.

- [ ] **Step 5: Clean up**

Confirm the branch is an ancestor of `main`, the worktree is clean, then remove `.worktrees/t32-closeout` and delete the local branch. Delete a remote branch only if one was pushed.

- [ ] **Step 6: Final closeout**

Ensure T3.2 is `DONE` with the evidence link in both campaign records. Report that T3.4 remains blocked on production recovery integration outside T3.2.
