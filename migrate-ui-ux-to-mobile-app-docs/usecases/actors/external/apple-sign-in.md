# External Actor — Apple Sign-In

**Type:** External system.

**Source evidence:** "Continue with Apple" button in `src/features/auth/LoginPage.jsx`.

**Used by domains:** `auth` only.

## Delegation edges

UC-A05 — Continue with Apple (`kind: "delegate"`).
