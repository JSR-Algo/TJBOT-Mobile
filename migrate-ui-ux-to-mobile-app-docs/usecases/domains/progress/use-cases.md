# Use Cases — `progress`

> **Owning lane:** Lane C. **UC count:** 5. Skeleton generated by Lane Z (Phase 0). Bodies filled by Lane C (Phase 1).
>
> Each H2 below corresponds to one UC ID from `reference/use-case-index.json`. Cross-domain edges: see `reference/cross-domain-edges.json`. Backend mapping: see `backend-mapping.md`. Edge cases: see `edge-cases.md`.

---

## UC-P01 — View Today's Progress

- **Goal:** Child sees a summary of today's activity (lessons completed, words practiced, time spent) so they can feel a sense of accomplishment.
- **Trigger:** Child navigates to the Progress tab from `home_hub_idle` or any bottom-nav entry point.
- **Preconditions:** Child is authenticated; at least one lesson session has been recorded for today.
- **Main Flow:**
  1. `TodayProgressPage` mounts and dispatches `getTodayProgress()` from `src/services/api/progress.api.js`.
  2. UI renders today's lesson count, word count, and session duration from the response.
  3. Child can tap "Words practiced" card to drill into UC-P02.
- **Postconditions:** Progress data is displayed; no state mutations occur (read-only view).

---

## UC-P02 — View Words Practiced

- **Goal:** Child reviews the specific vocabulary words practiced in today's sessions, reinforcing retention awareness.
- **Trigger:** Child taps the "Words practiced" drill-down from UC-P01 (`TodayProgressPage`).
- **Preconditions:** UC-P01 is displayed; `getWordsPracticed()` API is reachable.
- **Main Flow:**
  1. `WordsPracticedPage` mounts and calls `getWordsPracticed()` from `src/services/api/progress.api.js`.
  2. UI renders a list of words with their practice status (correct / needs review).
  3. If two or more words are flagged "needs review," a "Practice 2 words" CTA is shown (puml edge: `UC_PRG_WORDS ..> UC_PRG_REVIEW`).
  4. Child taps the CTA to enter UC-P04.
- **Postconditions:** Word list displayed; if CTA tapped, navigates to UC-P04.

---

## UC-P03 — View Lesson Summary

- **Goal:** Child (and optionally parent) sees a structured recap of the just-completed lesson — score, words covered, time taken.
- **Trigger:** Lesson session ends (`UC_LSN_DONE` event from lesson-session domain); app navigates to `progress_summary`.
- **Preconditions:** A lesson session has just concluded; `getLessonSummary()` API is reachable.
- **Main Flow:**
  1. `LessonSummaryPage` mounts with the completed session ID passed as route param.
  2. Calls `getLessonSummary()` from `src/services/api/progress.api.js` with session ID.
  3. Renders score (e.g. 4/5 words), time elapsed, and list of words covered.
  4. If any words need review, a "Review needed" badge is shown; tapping navigates to UC-P04.
  5. "Continue" CTA advances child to UC-P05 (celebration).
- **Postconditions:** Summary displayed; session is closed. Navigation state points to `progress_celebrate` or `progress_review`.

---

## UC-P04 — View Review Needed

- **Goal:** Child is directed to a focused review run for words that were answered incorrectly, improving retention before the next lesson.
- **Trigger:** Child taps the "Review needed" badge from UC-P03, or the "Practice 2 words" CTA from UC-P02.
- **Preconditions:** Review queue is non-empty (`getReviewQueue()` returns ≥ 1 word); child is authenticated.
- **Main Flow:**
  1. `ReviewNeededPage` mounts and calls `getReviewQueue()` from `src/services/api/progress.api.js`.
  2. UI lists words queued for review with a "Start review" CTA.
  3. Child taps "Start review" → navigates to lesson-session domain (`UC_LSN_START` review run — puml note `[exit] → lesson-session`).
- **Postconditions:** Child enters a review lesson session (UC-L01); progress domain is exited.
- **Alt Flow:**
  1. Review queue becomes empty (all words already mastered) → "Nothing to review" state with back navigation to `progress_today`.

---

## UC-P05 — View Persisted Reward Celebration

- **Goal:** Child receives positive reinforcement (animation, congratulatory message) after completing a lesson, motivating continued engagement.
- **Trigger:** Child taps "Continue" at the end of UC-P03 (`LessonSummaryPage`).
- **Preconditions:** The authenticated reward inbox contains the unseen persisted reward ID supplied by the completion flow.
- **Main Flow:**
  1. `LessonSummaryScreen` selects the unseen receipt by exact assignment ID and, when supplied by the route, exact session ID; an older unseen receipt for the same child and robot is never substituted.
  2. `CelebrationScreen` reads `GET /v1/mobile/rewards/inbox` and selects only the exact reward ID forwarded by the summary.
  3. It renders server XP, coins, badges, streak, child, robot, and reason; reduced-motion users receive a static decoration.
  4. It acknowledges that exact reward once through the idempotent seen endpoint; retryable offline acknowledgements enter the scoped seen queue.
  5. If the correlated persisted award is absent, the screen shows waiting-to-sync and never predicts a reward.
- **Postconditions:** The persisted reward is acknowledged or safely queued for acknowledgement; no award mutation occurs on mobile.

## UC-P06 — Review Private Rewards

- **Goal:** A parent reviews authoritative totals and history grouped and filtered by owned child and robot.
- **Trigger:** Parent opens Rewards from the gated parent summary or history surface.
- **Preconditions:** Parent is authenticated, a household is active, and parent-only navigation access has been established.
- **Main Flow:** Parent gate succeeds; the screen reads `GET /v1/mobile/rewards`, renders ledger reasons, and retains cached read-only data with an explicit offline/stale label.
- **Postconditions:** No reward balance is changed and no fabricated value is displayed.

## UC-P07 — Review Robot Leaderboard

- **Goal:** A parent compares opted-in robots while retaining visibility of every owned robot's private/current rank state.
- **Trigger:** Parent opens Leaderboard from private rewards or robot management.
- **Preconditions:** Parent is authenticated and an active household scope is available.
- **Main Flow:** Weekly/all-time tabs request bounded pages; refresh revalidates; public and owned rows render completed lessons, nullable/refreshing streak, and badge summary; owned rows outside the public page are merged and labelled with text plus border, not colour alone.
- **Postconditions:** Only backend-masked email is rendered; opted-out robots remain private while rewards persist.

## UC-P08 — Manage Reward Identity

- **Goal:** A parent renames an owned child, selects the active child after server confirmation, and controls each robot's public leaderboard preference.
- **Trigger:** Parent opens parent settings or the owned robot management screen.
- **Preconditions:** Parent is authenticated; target child or robot belongs to the active household.
- **Main Flow:** Parent settings uses the mobile child rename endpoint and active-child endpoint; robot settings renders every server-returned owned row, including opted-out robots, and writes each per-device preference without offline queuing. Device status not present in the response remains explicitly unavailable and is never fabricated.
- **Postconditions:** Server-confirmed household identity drives lessons, robot selection, rewards, and public visibility.
