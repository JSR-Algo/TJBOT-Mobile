{
  "name": "tbot-voice-native-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Expo config plugin that preserves the voice-stack Info.plist + AndroidManifest changes across `expo prebuild --clean`. See README.md.",
  "main": "app.plugin.js"
}
