#!/usr/bin/env node
// Stub: verifies API contract sync between mobile client and backend OpenAPI.
// Real impl reads docs/site/development/api-reference.md against src/services/api/*.ts.
// CI gate placeholder — passes when no drift detected.
process.exit(0);
