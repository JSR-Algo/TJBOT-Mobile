// Phantom route alias — points at the canonical RobotListeningScreen in the
// lesson-session feature. Maintained because some deep-links / older
// references navigate to `ListenScreen`. PR8 cleanup may remove this alias.
export { default } from '@/features/lesson-session/screens/RobotListeningScreen';
