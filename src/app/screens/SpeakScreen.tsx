// Phantom route alias — points at the canonical UserSpeakingScreen in the
// lesson-session feature. Maintained because some deep-links / older
// references navigate to `SpeakScreen`. PR8 cleanup may remove this alias.
export { default } from '@/features/lesson-session/screens/UserSpeakingScreen';
