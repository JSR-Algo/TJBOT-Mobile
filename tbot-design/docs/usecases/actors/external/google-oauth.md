# External Actor — Google OAuth

**Type:** External system.

**Source evidence:** "Continue with Google" button in `src/features/auth/LoginPage.jsx`.

**Used by domains:** `auth` only.

## Delegation edges

UC-A04 — Continue with Google (`kind: "delegate"`).
