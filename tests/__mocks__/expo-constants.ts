/** Shared Jest stub — matches fields used by diagnosticLog export metadata. */
const Constants = {
  expoConfig: { version: '1.0.0', ios: { buildNumber: '1' } },
  nativeAppVersion: '1.0.0',
  nativeBuildVersion: '1',
};

export default Constants;